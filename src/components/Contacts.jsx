import { Link } from 'react-router-dom';

export const Contacts = () => {
    return (
      <div>
        <button>
          <Link to="/">Go back</Link>
        </button>
        <p>it`s Contacts page</p>
      </div>
    );
};
